var React = require('react');

var PickerPrevButton = React.createClass({
    render: function() {
        return (
            <button href="#" className="picker-prev picker-btn" tabIndex="-1"></button>
        );
    }
});

module.exports = PickerPrevButton;