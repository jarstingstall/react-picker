var React = require('react');

var PickerNextButton = React.createClass({
    render: function() {
        return (
            <button href="#" className="picker-next picker-btn" tabIndex="-1"></button>
        );
    }
});

module.exports = PickerNextButton;